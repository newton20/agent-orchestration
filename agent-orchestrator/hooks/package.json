{
  "name": "agent-orchestrator-hooks",
  "version": "0.1.0",
  "private": true,
  "description": "Unit 5 — SessionStart hook for the agent-orchestrator plugin. Reads the oldest-fresh `.pending-<id>` flag file under docs/orchestration/ and injects its contents as the initial session context. No-op when no flag file is present.",
  "scripts": {
    "test": "node --test session-start.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
