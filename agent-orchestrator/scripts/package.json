{
  "name": "agent-orchestrator-scripts",
  "version": "0.1.0",
  "private": true,
  "description": "Node.js scripts consumed by the agent-orchestrator plugin: manifest parser/validator, session spawner, prompt generator, health checker, orchestrator event loop.",
  "bin": {
    "parse-manifest": "./parse-manifest.js",
    "scaffold-protocol": "./scaffold-protocol.js",
    "spawn-session": "./spawn-session.js",
    "generate-prompt": "./generate-prompt.js",
    "check-health": "./check-health.js",
    "orchestrate": "./orchestrate.js"
  },
  "scripts": {
    "test": "node --test parse-manifest.test.js scaffold-protocol.test.js spawn-session.test.js generate-prompt.test.js check-health.test.js orchestrate.test.js"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
